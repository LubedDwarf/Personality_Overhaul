pop_character_passes_performance_filter = {
	is_alive = yes
	is_landed = yes
	highest_held_title_tier >= tier_county

	trigger_if = {
		limit = { is_ai = yes }
		# Evaluates the internal parity index parameters
		has_character_flag = pop_allow_cyclical_evaluation_tick
	}
}
